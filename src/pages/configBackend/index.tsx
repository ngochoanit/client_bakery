import React from 'react';
import ConfigBackend from 'src/containers/configBackend';

const configBackendPage = function configBackendPage() {
  return <ConfigBackend />;
};

export default configBackendPage;
